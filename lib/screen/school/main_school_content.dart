import 'package:flutter/material.dart';

import '../../res/strings.dart';

class SchoolContet extends StatelessWidget {
  const SchoolContet();
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Index 2: School',
        style: AppStrings.TITLE_STYLE,
      ),
    );
  }

}